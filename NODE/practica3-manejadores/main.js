//Declaramos un manejador de enrutamientos de las url de las peticiones a nuestro servidor.
const routerIgae = ( pathPeticion ) => {
    console.log('Petición enrutada para ' + pathPeticion);
}

exports.routerIgae = routerIgae;