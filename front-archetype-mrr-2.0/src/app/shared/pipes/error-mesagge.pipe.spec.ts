import { ErrorMesaggePipe } from './error-mesagge.pipe';

describe('ErrorMesaggePipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorMesaggePipe();
    expect(pipe).toBeTruthy();
  });
});
