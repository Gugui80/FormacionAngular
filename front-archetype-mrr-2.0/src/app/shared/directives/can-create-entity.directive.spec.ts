import { CanCreateEntityDirective } from './can-create-entity.directive';

describe('CanCreateEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new CanCreateEntityDirective();
    expect(directive).toBeTruthy();
  });
});
