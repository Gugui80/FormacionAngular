import { CanEditEntityDirective } from './can-edit-entity.directive';

describe('CanEditEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new CanEditEntityDirective();
    expect(directive).toBeTruthy();
  });
});
