import { CanReadEntityDirective } from './can-read-entity.directive';

describe('CanReadEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new CanReadEntityDirective();
    expect(directive).toBeTruthy();
  });
});
