import { CanDeleteEntityDirective } from './can-delete-entity.directive';

describe('CanDeleteEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new CanDeleteEntityDirective();
    expect(directive).toBeTruthy();
  });
});
